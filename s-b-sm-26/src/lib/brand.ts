/** Intrinsic GIF size and one full rotation (56 frames × 60 ms). */
export const LOGO_SPLASH_PX = 280;
/** Header mark — large enough to read the knot on the black square. */
export const LOGO_HEADER_PX = 48;
export const LOGO_CYCLE_MS = 3360;
export const LOGO_FADE_MS = 500;
/** Matches Ironknot Mark-selection.png (black field). */
export const HEADER_BAR_BG = "#000000";
